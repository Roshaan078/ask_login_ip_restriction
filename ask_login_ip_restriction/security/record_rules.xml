<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- Users can only manage their own IP restrictions -->
        <record id="rule_user_ip_restriction_own" model="ir.rule">
            <field name="name">User IP Restriction - Own Records</field>
            <field name="model_id" ref="model_user_ip_restriction"/>
            <field name="domain_force">[('user_id', '=', user.id)]</field>
            <field name="groups" eval="[(4, ref('base.group_user'))]"/>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="True"/>
            <field name="perm_create" eval="True"/>
            <field name="perm_unlink" eval="False"/>
        </record>

        <!-- System administrators have full access -->
        <record id="rule_user_ip_restriction_admin" model="ir.rule">
            <field name="name">User IP Restriction - Admin Full Access</field>
            <field name="model_id" ref="model_user_ip_restriction"/>
            <field name="domain_force">[(1, '=', 1)]</field>
            <field name="groups" eval="[(4, ref('base.group_system'))]"/>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="True"/>
            <field name="perm_create" eval="True"/>
            <field name="perm_unlink" eval="True"/>
        </record>
    </data>
</odoo>
